% mutate.m Mutates inserted individuals
%    newPopulation = mutate (population, inserted, param)
%
%    @param cell array population:
%    @param vector of int, inserted: indexies into previous parameter marking inserted pffspring
%    @param struct, param: main parameters
%    @return cell array newPopulation:
%
%    Performes all different mutations. 

function rPop = mutate (aPop, aInserted, aParam)

return;
